# Third-Party Notices

Plainspace uses open-source dependencies listed in `package-lock.json`.

The current dependency tree contains permissive licenses only, including MIT,
Apache-2.0, BSD-2-Clause, BSD-3-Clause, ISC, 0BSD, MIT-0, Unlicense, Python-2.0,
BlueOak-1.0.0, and CC-BY-4.0 for the dev-only `caniuse-lite` browser-compat data
package.

Before distributing dependency archives, container images with vendored
dependencies, or offline builds, regenerate this notice from the lockfile and
include required license texts and attributions.
